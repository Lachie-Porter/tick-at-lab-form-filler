javascript:(function () { function run($) { var DATA = JSON.parse(localStorage.getItem('bm_csv_data') || 'null'); if (!DATA) { alert('No CSV loaded. Use the Load CSV bookmarklet first.'); return; } var KEY = 'bm_progress'; localStorage.removeItem(KEY); var state = null; if (!state) { var procs = Object.keys(DATA); var menu = procs.map(function (p, i) { return (i + 1) + ' = ' + p; }).join('\n'); var pick = prompt('Which procedure?\n' + menu); if (!pick) return; var expName = procs[parseInt(pick.trim(), 10) - 1]; if (!expName) { alert('Unknown selection'); return; } state = { exp: expName, index: 0 }; } function byLabel(form, text) { return form.find('label').filter(function () { return $(this).clone().children().remove().end().text().trim() === text; }).find('input, select, textarea'); } function fillNext(form) { var list = DATA[state.exp]; if (state.index >= list.length) { alert('Done — ' + state.exp + ' complete!'); localStorage.removeItem(KEY); return; } var person = list[state.index]; byLabel(form, 'Investigator name').val(person.name); byLabel(form, 'Procedure name').val(state.exp); byLabel(form, 'Species').val('Mouse'); byLabel(form, 'Competency').val(person.competency); state.index++; localStorage.setItem(KEY, JSON.stringify(state)); document.title = '(' + state.index + '/' + list.length + ') ' + state.exp; } $(document).off('click.bm').on('click.bm', 'a', function () { if ($(this).text().trim() === 'Apply & New') { fillNext($('[id*="AdditionalProcedureTrainingDetailsTable"]').first()); } }); fillNext($('[id*="AdditionalProcedureTrainingDetailsTable"]').first()); } if (window.jQuery) { run(window.jQuery); } else { var s = document.createElement('script'); s.src = 'https://code.jquery.com/jquery-3.7.1.min.js'; s.onload = function () { run(window.jQuery); }; document.head.appendChild(s); } })();